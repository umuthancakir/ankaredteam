<!doctype html>
<html lang="tr">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Hacked By TurkHackTeam | Anka Red Team</title>
<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Iceland">
<style>
  :root{
    --red:#ff1a1a;
    --text-shadow-1:0 0 6px rgba(255,20,20,0.9);
    --text-shadow-2:0 0 18px rgba(255,30,30,0.7);
    --text-shadow-3:0 0 36px rgba(255,50,50,0.45);
  }
  html,body{
    height:100%;margin:0;
    background:radial-gradient(ellipse at center,#050505 0%,#000 60%);
    color:#fff;font-family:'Iceland',sans-serif;
    overflow-x:hidden;
  }
  .wrap{
    min-height:100%;
    display:flex;align-items:center;justify-content:center;
    flex-direction:column;text-align:center;
    padding:40px 20px;box-sizing:border-box;
  }
  .logo-wrap{
    position:relative;display:inline-block;
    padding:16px;border-radius:18px;margin-bottom:12px;
  }
  .logo-wrap::before{
    content:"";position:absolute;left:-8px;right:-8px;top:-8px;bottom:-8px;
    border-radius:22px;pointer-events:none;z-index:-1;
    box-shadow:0 0 12px rgba(255,0,0,0.9),0 0 30px rgba(255,20,20,0.6),0 0 60px rgba(255,40,40,0.35);
    filter:blur(2px);
  }
  .logo{
    display:block;width:360px;max-width:82vw;height:auto;border-radius:6px;
    box-shadow:0 10px 30px rgba(0,0,0,0.8);
    filter:drop-shadow(0 0 10px rgba(255,0,0,0.85));
  }
  .neon{
    color:var(--red);
    margin:6px 0;line-height:1.05;
    text-shadow:0 0 8px rgba(255,30,30,0.95),0 0 22px rgba(255,40,40,0.85),0 0 40px rgba(255,80,80,0.5);
  }
  h1.neon{font-size:2.8rem;margin-top:8px;}
  h2.neon{font-size:2rem;margin:6px 0;}
  p.neon-small{font-size:1.2rem;margin:6px 0 0;}
  .partner{
    display:inline-block;margin-top:8px;font-size:1.1rem;
    text-decoration:none;color:var(--red);
    text-shadow:var(--text-shadow-1),var(--text-shadow-2);
  }
  @keyframes flickerText{
    0%,100%{opacity:1;filter:brightness(1);text-shadow:var(--text-shadow-1),var(--text-shadow-2),var(--text-shadow-3);}
    3%{opacity:.2;filter:brightness(.3);text-shadow:none;}
    6%{opacity:1;}
    9%{opacity:.1;}
  }
  @keyframes flickerLogo{
    0%,100%{opacity:1;transform:scale(1);}
    5%{opacity:.1;transform:scale(.98);}
    8%{opacity:1;transform:scale(1);}
  }
  body.flicker .neon,body.flicker .partner{animation:flickerText 2.6s infinite;}
  body.flicker .logo{animation:flickerLogo 2.6s infinite;}
  .blackout{
    position:fixed;left:0;right:0;top:0;bottom:0;background:#000;
    pointer-events:none;opacity:0;transition:opacity .15s ease;z-index:80;
  }
  .blackout.on{opacity:1;transition:opacity .05s ease;}
  .hint{font-size:.8rem;color:#bbb;margin-top:14px;opacity:.28;text-shadow:none;}
  @media(max-width:520px){
    .logo{width:80vw;}
    h1.neon{font-size:2.2rem;}
    h2.neon{font-size:1.6rem;}
  }
</style>
</head>
<body onclick="playAudio();" class="flicker">
  <div class="wrap">
    <div class="logo-wrap">
      <img class="logo" src="http://zonehmirrors.org/defaced/2021/01/27/wothoq.co/i.hizliresim.com/m9RCVr.png" alt="logo" />
    </div>
    <h1 class="neon">Hello Admin!</h1>
    <h2 class="neon"><b>Anka Red Team Was Here :)</b></h2>
    <p class="neon-small">
      <a class="partner" href="https://turkhackteam.org/" target="_blank" rel="noopener noreferrer">[ Turkhackteam.org ]</a>
    </p>
    <p class="hint">Sayfaya tıklayın — ses çalacak.</p>
  </div>
  <div id="blackout" class="blackout"></div>
  <audio id="sec">
    <source src="https://cdn.discordapp.com/attachments/1025874740847575164/1025875083190874212/mt-hani-gokboru-beat.mp3" type="audio/mpeg">
  </audio>
  <script>
    var x=document.getElementById("sec");
    function playAudio(){
      if(x&&x.paused){x.play().catch(()=>{});}
    }
    (function(){
      var body=document.body,blackout=document.getElementById('blackout');
      body.classList.add('flicker');
      function maybeBlackout(){
        if(Math.random()<0.25){
          var dur=200+Math.random()*900;
          blackout.classList.add('on');
          body.classList.remove('flicker');
          setTimeout(function(){
            blackout.classList.remove('on');
            setTimeout(function(){body.classList.add('flicker');},50+Math.random()*120);
          },dur);
        }
        var next=1000+Math.random()*3000;
        setTimeout(maybeBlackout,next);
      }
      setTimeout(maybeBlackout,1200+Math.random()*800);
    })();
  </script>
</body>
</html>
